@Library('jenkins-shared-library') _
pipeline {
    agent any
    stages {
        stage('Greet User') {
            steps {
                script {
                    greet('Jenkins User')
                }
            }
        }
    }
}
